import React from 'react';
import {Stylessheet,
Text,
View,
Textinput,
ScrollView,
TouchableOpacity,
} from 'react-native';

import { render } from 'react-dom';

export default class HomeScreeen extends React.DOM {
    render() 
        return (
              <View>
                <Text>- Seenyx -</Text>
              </View>
        );
    }

const Styles = Stylesheets.create({
    container: {

    },
    });